// << = stream insertion operator , inserts whatever is to the right of the operator into the the console
// >> = stream extraction operator , extracts from the console and inserts in to a variable
// :: = scope resolution operator , resolves which namespace scope to use
// namespace = names given to parts of code to help reduce naming conflicts

// preprocessor processes source code before the compiler, it replaces all comments with a space and removes preprocessor directives
// preprocessor does not understand C++
// # = preprocessor directive

// using namespace std; = takes in the entire std namespace
// using std::cout; = only uses what is needed in the std namespace

int main(int argc, char *argv[])
{
    // int argc is the argument count
    // char *argv[] is an array of the passed in arguments
    // code
    return 0;
}

// program1.exe arg1 arg2
